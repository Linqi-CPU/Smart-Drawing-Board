# 使用说明

## 启动方式

推荐使用 **启动器（Launcher）**，它可以统一管理内核和所有功能页面：

1. 解压 Release 中的 `SmartDrawingBoard-Launcher.zip`
2. 运行 `SmartDrawingBoard-Launcher.exe`
3. 在启动器窗口中点击「打开」启动对应功能

启动器会确保计算内核在后台运行，并在状态栏显示内核状态和在线页面数量。

---

## 主绘图板

### 基本操作

| 操作 | 说明 |
|------|------|
| 左键拖动 | 在画布上手绘 |
| 自动按钮 | 按选择的函数类型自动绘制曲线 |
| 清除按钮 | 清空画布，保留 Y 上限线 |
| 停止按钮 / Esc | 停止当前绘制动画 |
| 导出 PNG | 保存画布内容为图片 |
| 帮助 | 打开 math 函数参考 |

### 控制面板说明

- **函数类型**：正弦、余弦、抛物线、直线、自定义
- **振幅**：控制曲线高度
- **频率**：控制曲线疏密
- **动画速度**：控制自动绘线的快慢
- **Y 上限**：红色虚线标注的位置，随窗口拉伸自动更新

### 滑动值计算

手绘时左侧面板实时显示累计滑动值（折线长度），单位为像素。

---

## 自定义函数

### 编写自定义函数

1. 在「自定义函数」面板输入函数名称（如 `mywave`）
2. 在代码框输入函数体
3. 点击「保存函数」
4. 将函数类型切换为「自定义」
5. 点击「自动按钮」查看效果

### 函数签名要求

```python
def 函数名(x, width, center_y, amp, freq):
    """
    x       当前 x 坐标（0 ~ width，像素）
    width   画布宽度（像素）
    center_y 画布垂直中心（像素）
    amp     振幅滑块当前值
    freq    频率滑块当前值

    返回：屏幕 y 坐标（像素，向下为正）
    """
    return center_y + amp * math.sin(2 * math.pi * freq * x / width)
```

**注意**：
- 函数名必须符合 Python 标识符规则
- 必须恰好 5 个参数
- 返回值为屏幕 y 坐标，向下为正

### 示例函数

**阻尼振荡**：
```python
import math

def damped(x, width, center_y, amp, freq):
    t = x / width
    return center_y + amp * (1 - t) * math.sin(2 * math.pi * freq * t)
```

**方波**：
```python
import math

def square(x, width, center_y, amp, freq):
    t = x / width
    s = math.sin(2 * math.pi * freq * t)
    v = 1.0 if s > 0 else (-1.0 if s < 0 else 0.0)
    return center_y + amp * v
```

### 安全限制

自定义函数会经过 AST 静态校验，禁止以下操作：
- 文件访问：`open()`、`os`、`sys`
- 进程执行：`subprocess`、`eval()`、`exec()`
- 网络操作：`socket`、`urllib`
- 反射逃逸：`getattr`、`__import__`、私有属性访问

运行期还有容错保护：单点异常或返回 `NaN`/`inf` 时自动回落到中心线，不会中断整条曲线。

---

## 分段包络估计

### 功能入口

通过启动器打开「分段包络估计走势与离散分析」页面。

### 操作步骤

1. **导入数据**：点击「导入」选择 Excel/CSV/TSV/TXT 文件，或直接在画布上采集点
2. **设置参数**：
   - **拟合阶数**：多项式阶数（0-20）
   - **分段数**：将数据分成多少段估计
3. **执行估计**：点击「估计」按钮
4. **查看结果**：
   - 上界/下界拟合公式
   - 走势图与离散宽度图
   - 文本报告

### 输出说明

- **上界拟合**：估计散点的上包络线
- **下界拟合**：估计散点的下包络线
- **离散宽度**：上下界之间的宽度序列，反映数据波动程度

---

## Excel 数据导入

支持格式：`.xlsx`、`.csv`、`.tsv`、`.txt`

### 导入规则

- 第一行作为表头
- 第一列作为 X 值
- 第二列作为 Y 值
- 后续列可作为多变量拟合的输入

### 多变量拟合

导入多列数据后，程序会按表头数据名生成拟合表达式，例如：
```
销售额 = 12.5 * 广告投入 + 3.2 * 季节指数 + 常数项
```

---

## 导出功能

### 导出 PNG

点击「导出 PNG」按钮，画布内容（含 Y 上限线、拟合曲线、散点）会保存为图片。

**注意**：导出的是画布区域，不含左右侧边面板。

---

## 环境变量

| 变量 | 默认值 | 说明 |
|------|--------|------|
| `HERMES_KERNEL_PORT` | `8765` | 内核服务端口 |
| `HERMES_KERNEL_HOST` | `127.0.0.1` | 内核服务地址 |
| `HERMES_KERNEL_OUTPUT_ROOT` | `D:\.hermes_kernel` | 产物输出目录 |
| `DRAWING_FUNCTIONS_DIR` | `./functions` | 自定义函数目录 |

---

## 常见问题

**Q：点击「自动按钮」提示函数不存在？**  
检查函数名称是否与文件名一致，且文件内有同名函数。

**Q：曲线画出来只有一条直线？**  
可能函数类型选到了「直线」，或自定义函数返回了常数。

**Q：自定义函数一切换就白屏？**  
函数返回值可能超出画布范围，检查是否返回了 `NaN` 或极大值。建议使用有界函数。

**Q：窗口拉伸后曲线变形？**  
自动曲线按绘制时的画布尺寸采样，拉伸后位置会失真。重新点击「自动按钮」即可按新尺寸重绘。

**Q：导出的 PNG 只有画布？**  
这是预期行为，导出的是画布区域，不含侧边面板。
